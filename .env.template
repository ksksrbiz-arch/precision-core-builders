# ═════════════════════════════════════════════════════════════
# DEPRECATED — use `.env.example` instead
# ═════════════════════════════════════════════════════════════
#
# This file is kept only so older setup scripts/docs that still
# reference `.env.template` do not break. The canonical, complete
# environment variable list lives in:
#
#   .env.example
#
# Copy that file to `.env.local` for local development, and set the
# same keys in the Netlify dashboard for production.
#
# Do NOT put real secrets in this file.
